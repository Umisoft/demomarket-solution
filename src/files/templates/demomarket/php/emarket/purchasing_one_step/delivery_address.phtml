<?php
/**
 * Выбор адреса доставки.
 *
 * @var umiTemplaterPHP|ViewPhpExtension|DemomarketPhpExtension $this
 * @var array $variables
 */

if (!isset($variables['onestep']['delivery'])) {
	return;
}

$variables = $variables['onestep']['delivery'];
$variables['isRenderSaveButton'] = true;

?>

<?= $this->render($variables, 'emarket/delivery/address/block') ?>
