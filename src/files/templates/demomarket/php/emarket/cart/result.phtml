<?php
/**
 * Блок "Оформить заказ".
 *
 * @var umiTemplaterPHP|ViewPhpExtension|DemomarketPhpExtension $this
 * @var array $variables
 */
$drawFormButton = isset($variables['drawFormButton']) ? $variables['drawFormButton'] : false;
$isPromoCodesAvailable = $this->macros('emarket', 'isPromoCodesAvailable');
?>

<div class="order_result">
	<div class="container mobile_all_width">
		<div class="row background_gray">
			<?php if ($isPromoCodesAvailable): ?>
				<?= $this->render($variables, 'emarket/cart/promocode') ?>
				<div class="col-sm-12 col-lg-6 order_pricing">
			<?php else: ?>
				<div class="col-sm-12 col-lg-offset-7 col-lg-4 order_pricing">
			<?php endif; ?>

				<p>
					<?=$this->translate('order-discount')?>:

					<span id="order_discount">
						<?= $this->getOrderDiscount($variables) ?>
					</span>
				</p>

				<p>
					<?=$this->translate('total-discount')?>:

					<span id="total_discount">
						<?= $this->getTotalDiscount($variables) ?>
					</span>
				</p>

				<p class="result_sum">
					<?= $this->translate('summary-price') ?>:
					<span id="order_price">
						<?= $this->getOrderPrice($variables) ?>
					</span>
				</p>

				<?php if($drawFormButton): ?>
					<p>
						<input type="submit" value="<?= $this->translate('begin_purchase') ?>">
					</p>

					<?= $this->render('purchase-step-order-federal-law', 'library/personalDataNotice') ?>
				<?php else: ?>
					<a href="<?= $this->macros('emarket', 'getPurchaseLink') ?>" class="blue_btn">
						<?= $this->translate('begin_purchase') ?>
					</a>
				<?php endif; ?>
			</div>
		</div>
	</div>
</div>
